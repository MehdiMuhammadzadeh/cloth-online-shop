// import { useEffect } from "react";
// import { getRedirectResult } from "firebase/auth";



  // const logGoogleRedirectUser = async () => {
  //   const { user } = await signInWihGoogleRefirect();
  //   console.log({ user });
  // };

// signInWihGoogleRedirect,
// auth,
// In the top of the componet right below the signin component we have the useEffect
// useEffect(() => {
//   async function fetchResponse() {
//     const response = await getRedirectResult(auth);
//     if (response) {
//       const userDocRef = await createUserDocumentFromAuth(response.user);
//     }
//     // console.log(response);
//   }
//   fetchResponse();
// });

// And this button to show that we can use both the redirect and popup method here throught this long journey
//   <button onClick={signInWihGoogleRedirect}>
//   Sign In With Google Redirect
// </button>

